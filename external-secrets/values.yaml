replicaCount: 2

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

serviceAccount:
  create: true
  name: external-secrets

webhook:
  replicaCount: 2

certController:
  replicaCount: 2
