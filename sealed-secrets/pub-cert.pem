# Placeholder — replace with the actual certificate fetched from your cluster:
#   chmod +x sealed-secrets/fetch-cert.sh
#   ./sealed-secrets/fetch-cert.sh
#
# The cert is cluster-specific. If the controller is re-deployed and key rotation
# occurs, re-run fetch-cert.sh and re-seal all SealedSecrets.
